{
  "name": "openbrain-mcp-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "MCP Gateway for the self-hosted OpenBrain stack — hosts local MCP servers",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
