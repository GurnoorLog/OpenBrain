{
  "name": "openbrain-cloud-executor",
  "version": "1.0.0",
  "private": true,
  "description": "Runs OpenBrain graphs server-side on Render (on-demand /run + scheduled workflow)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "workflow": "node workflow.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
